<!DOCTYPE html>
<html lang="ko">
<head>
    {{> layout/header }}
</head>
<body>
<h1>게시글 수정</h1>
<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div>
                글 번호&emsp;&emsp;<span class="fw-bold">{{ post.id }}</span>
            </div>
            <div class="form-group">
                <label for="title">제목</label>
                <input type="text" id="title" class="form-control" value="{{ post.title }}">
            </div>
            <div class="form-group">
                작성자&emsp;&emsp;<span class="fw-bold">{{ post.author }}</span>
            </div>
            <div class="form-group">
                <label for="content">내용</label>
                <textarea id="content" class="form-control">{{ post.content }}</textarea>
            </div>
        </form>

        <a href="/" role="button" class="btn btn-secondary">취소</a>
        <button type="button" id="btn-update" class="btn btn-primary">수정 완료</button>
        <button type="button" id="btn-delete" class="btn btn-danger">삭제</button>
    </div>
</div>

{{> layout/footer }}
<script language="JavaScript">const postId = {{ post.id }}, userId = {{ userId }};
</script>
</body>
</html>